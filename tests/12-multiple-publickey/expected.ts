import { address } from '@solana/kit';
import { PublicKey } from "@solana/web3.js";

const a = address("aaa");
const b = address("bbb");
const c = address(rawBytes);
const arr = [address("x"), address("y")];
