diff --git a/src/lib/keypair.ts b/src/lib/keypair.ts
index 2801c1e..7dd494f 100644
--- a/src/lib/keypair.ts
+++ b/src/lib/keypair.ts
@@ -1,11 +1,12 @@
+import { generateKeyPairSigner, createKeyPairSignerFromBytes, KeyPairSigner } from '@solana/kit';
 import { Keypair } from "@solana/web3.js";
 import base58 from "bs58";
 
 // Default value from Solana CLI
 const DEFAULT_FILEPATH = "~/.config/solana/id.json";
 
-export const keypairToSecretKeyJSON = (keypair: Keypair): string => {
-  return JSON.stringify(Array.from(keypair.secretKey));
+export const keypairToSecretKeyJSON = (keypair: KeyPairSigner): string => {
+  return JSON.stringify(Array.from(keypair.privateKey));
 };
 
 export const getKeypairFromFile = async (filepath?: string) => {
@@ -44,7 +45,7 @@ export const getKeypairFromFile = async (filepath?: string) => {
     }
     throw new Error(`Invalid secret key file at '${filepath}'!`);
   }
-  return Keypair.fromSecretKey(parsedFileContents);
+  return await createKeyPairSignerFromBytes(parsedFileContents);
 };
 
 export const getKeypairFromEnvironment = (variableName: string) => {
@@ -57,7 +58,7 @@ export const getKeypairFromEnvironment = (variableName: string) => {
   let decodedSecretKey: Uint8Array;
   try {
     decodedSecretKey = base58.decode(secretKeyString);
-    return Keypair.fromSecretKey(decodedSecretKey);
+    return await createKeyPairSignerFromBytes(decodedSecretKey);
   } catch (throwObject) {
     const error = throwObject as Error;
     if (!error.message.includes("Non-base58 character")) {
@@ -75,11 +76,11 @@ export const getKeypairFromEnvironment = (variableName: string) => {
       `Invalid secret key in environment variable '${variableName}'!`,
     );
   }
-  return Keypair.fromSecretKey(decodedSecretKey);
+  return await createKeyPairSignerFromBytes(decodedSecretKey);
 };
 
 export const addKeypairToEnvFile = async (
-  keypair: Keypair,
+  keypair: KeyPairSigner,
   variableName: string,
   envFileName?: string,
 ) => {
@@ -95,11 +96,11 @@ export const addKeypairToEnvFile = async (
   const secretKeyString = keypairToSecretKeyJSON(keypair);
   await appendFile(
     envFileName,
-    `\n# Solana Address: ${keypair.publicKey.toBase58()}\n${variableName}=${secretKeyString}`,
+    `\n# Solana Address: ${keypair.address.toBase58()}\n${variableName}=${secretKeyString}`,
   );
 };
 
 // Shout out to Dean from WBA for this technique
-export const makeKeypairs = (amount: number): Array<Keypair> => {
-  return Array.from({ length: amount }, () => Keypair.generate());
+export const makeKeypairs = (amount: number): Array<KeyPairSigner> => {
+  return Array.from({ length: amount }, () => await generateKeyPairSigner());
 };
