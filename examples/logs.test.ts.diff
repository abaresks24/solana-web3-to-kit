diff --git a/tests/src/logs.test.ts b/tests/src/logs.test.ts
index 444696b..771a054 100644
--- a/tests/src/logs.test.ts
+++ b/tests/src/logs.test.ts
@@ -1,3 +1,5 @@
+import { generateKeyPairSigner, createSolanaRpc } from '@solana/kit';
+import { getTransferSolInstruction } from '@solana-program/system';
 import { describe, test } from "node:test";
 import { Connection } from "@solana/web3.js";
 import { airdropIfRequired, getCustomErrorMessage, getLogs } from "../../src";
@@ -11,26 +13,22 @@ const LOCALHOST = "http://127.0.0.1:8899";
 
 describe("getLogs", () => {
   test("getLogs works", async () => {
-    const connection = new Connection(LOCALHOST);
-    const [sender, recipient] = [Keypair.generate(), Keypair.generate()];
+    const connection = createSolanaRpc(LOCALHOST);
+    const [sender, recipient] = [await generateKeyPairSigner(), await generateKeyPairSigner()];
     const lamportsToAirdrop = 2 * LAMPORTS_PER_SOL;
     await airdropIfRequired(
       connection,
-      sender.publicKey,
+      sender.address,
       lamportsToAirdrop,
       1 * LAMPORTS_PER_SOL,
     );
 
     const transaction = await connection.sendTransaction(
       new Transaction().add(
-        SystemProgram.transfer({
-          fromPubkey: sender.publicKey,
-          toPubkey: recipient.publicKey,
-          lamports: 1_000_000,
-        }),
+        getTransferSolInstruction({ source: sender.address, destination: recipient.address, amount: 1_000_000 }),
       ),
       [sender],
-    );
+    ).send();
 
     const logs = await getLogs(connection, transaction);
     assert.deepEqual(logs, [
